apiVersion: v1
kind: Service
metadata:
  name: ecommerce-streamlit-service
  labels:
    app: ecommerce-streamlit
spec:
  selector:
    app: ecommerce-streamlit
  ports:
    - name: http
      protocol: TCP
      port: 8501
      targetPort: 8501
  type: ClusterIP
